# ``DesignSystemTestSupport``

Validate app-owned design-system conventions through the public capability interfaces.

These Swift Testing helpers enumerate the app's finite token vocabularies and report issues in the
consumer's test process. They do not modify production values or establish app-specific policy.

- <doc:ValidationGuide>
