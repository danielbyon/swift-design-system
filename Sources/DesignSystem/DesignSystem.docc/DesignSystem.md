# ``DesignSystem``

Build app-owned design vocabularies on small, strongly typed capability interfaces.

The package supplies color, gradient, and scalar resolution mechanics. An application owns its
token names, values, themes, environment integration, and visual identity.

- <doc:ApplicationIntegration>
